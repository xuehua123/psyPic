# 11. 技术选型与项目结构方案

## 默认技术栈

除非用户明确指定其他栈，PsyPic 默认使用：

- Frontend / BFF：Next.js App Router + TypeScript。
- 包管理：pnpm。
- UI：Tailwind CSS + shadcn/ui 风格组件 + lucide 图标。
- 表单：React Hook Form + Zod。
- 数据请求：TanStack Query。
- 本地历史缓存：IndexedDB，建议使用 Dexie。
- 数据库：PostgreSQL。
- ORM：Prisma 或 Drizzle，二选一即可；默认推荐 Prisma，便于快速建模和迁移。
- Redis：存 session、import code、限流、任务锁。
- 对象存储：Cloudflare R2 / S3 / MinIO，接口按 S3 兼容设计。
- 后台任务：MVP 先同步请求；`v0.5` 后引入轻量 job table + worker。
- 部署：先支持单体部署；后续可拆 Web/BFF/Worker。

## 为什么默认 Next.js

- 创作台和社区都需要高质量前端体验。
- BFF 可以和前端同仓，降低密钥代持、图片代理和 session 开发成本。
- Route Handlers 可承载 `/api/images/*`、导入交换、历史、模板和社区 API。
- 后续做灵感社区时可以逐步引入 SSR/ISR，不需要重构应用形态。

不建议 MVP 使用纯前端 SPA 直连 Sub2API，因为生产环境需要 BFF 代持密钥、统一参数校验、限流、错误标准化和审计。

## 推荐目录结构

```text
psypic/
  app/
    (creator)/
      page.tsx
      layout.tsx
    inspiration/
      page.tsx
      [workId]/page.tsx
    templates/
      page.tsx
    settings/
      page.tsx
    api/
      import/
        exchange/route.ts
      images/
        generations/route.ts
        edits/route.ts
      assets/
        [assetId]/route.ts
      history/route.ts
      templates/route.ts
      community/
        works/route.ts
        works/[workId]/route.ts
        works/[workId]/same/route.ts
        works/[workId]/favorite/route.ts
        reports/route.ts
  components/
    creator/
    editor/
    history/
    templates/
    community/
    layout/
    ui/
  lib/
    api/
    auth/
    sub2api/
    image/
    storage/
    billing/
    moderation/
    validation/
  server/
    services/
    repositories/
    jobs/
  prisma/
    schema.prisma
    migrations/
  docs/
  public/
```

如果选择 Drizzle，将 `prisma/` 换成 `drizzle/`，其余边界不变。

## 模块边界

### UI 层

- 只处理交互、展示和表单状态。
- 不持有长期 Sub2API API Key。
- 不直接调用 Sub2API。
- 调用 PsyPic 自己的 `/api/*`。

### BFF API 层

- 校验 session。
- 查找 Sub2API key binding。
- 校验参数。
- 调用 Sub2API。
- 标准化错误、usage、request id。
- 写入任务、历史、成本和审计记录。

### Service 层

- `ImageService`：文生图、图生图、同款生成。
- `ImportService`：Sub2API ticket/import code 交换。
- `TemplateService`：商业模板。
- `CommunityService`：作品发布、详情、互动、举报。
- `BillingService`：成本估算和实际 usage 记录。
- `ModerationService`：发布审核、下架、风险提示。

### Repository 层

- 只处理数据库读写。
- 不直接调用 Sub2API。
- 不做 UI 或 HTTP 逻辑。

## 环境变量

```env
APP_URL=https://psypic.example.com
DATABASE_URL=postgresql://...
REDIS_URL=redis://...
SESSION_SECRET=...
KEY_ENCRYPTION_SECRET=...
SUB2API_DEFAULT_BASE_URL=https://sub2api.example.com/v1
S3_ENDPOINT=...
S3_REGION=auto
S3_BUCKET=psypic
S3_ACCESS_KEY_ID=...
S3_SECRET_ACCESS_KEY=...
MAX_IMAGE_UPLOAD_MB=20
TEMP_ASSET_TTL_HOURS=72
ENABLE_COMMUNITY=false
ENABLE_PUBLIC_PUBLISH=false
```

安全要求：

- `SESSION_SECRET` 和 `KEY_ENCRYPTION_SECRET` 必须不同。
- API Key 不得写入前端环境变量。
- 生产日志必须脱敏 `Authorization`、`api_key`、`psypic_session`。

## 阶段落地策略

### `v0.1-v0.2`

- 项目骨架。
- BFF route handlers。
- 手动配置开发模式。
- Sub2API 一键导入契约。
- session 和 key binding 后端代持。

### `v0.3-v0.4`

- 文生图、图生图、单参考图。
- IndexedDB 基础历史。
- 商业尺寸预设和单目标尺寸生成/下载。
- MVP 验收闭环。

### `v0.5-v0.7`

- 流式 partial preview。
- 任务队列和取消。
- 服务端任务表。
- 服务端历史、素材库、收藏、ZIP。

### `v0.8-v1.0`

- 商业模板和 Prompt 助手。
- 社区最小发布、审核、举报、下架。
- 灵感社区信息流、作品详情、同款生成。
- 批量任务、CSV、多尺寸批量。

## 部署策略

MVP 推荐单体部署：

```text
Next.js app
  -> Route Handlers as BFF
  -> PostgreSQL
  -> Redis
  -> S3/R2
  -> Sub2API
```

正式版可拆成：

```text
Web/BFF
Worker
PostgreSQL
Redis
Object Storage
Sub2API
```

生产安全：

- 不在生产业务服务器构建 Docker 镜像。
- 生产只拉取预构建镜像、切换 tag、重启服务、健康检查和回滚。
- 发布前记录当前镜像 tag 和回滚命令。

## 开发命令建议

```bash
pnpm install
pnpm dev
pnpm lint
pnpm typecheck
pnpm test
pnpm build
```

如果使用 Prisma：

```bash
pnpm prisma generate
pnpm prisma migrate dev
```

## 开工前确认

- 是否接受默认 Next.js 技术栈。
- 数据库 ORM 使用 Prisma 还是 Drizzle。
- 对象存储使用 R2、S3 还是 MinIO。
- 部署平台是 Vercel、Cloudflare、Render、自有服务器还是其他。
- 是否需要从第一版就接入真实 PostgreSQL/Redis，还是 MVP 可用本地服务。
