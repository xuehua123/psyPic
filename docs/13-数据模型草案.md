# 13. 数据模型草案

## 设计原则

- 密钥、session、导入绑定必须后端保存。
- MVP 历史和图片预览可以先放 IndexedDB，但服务端仍要保存必要的导入绑定、审计和任务摘要。
- `v0.7` 后服务端保存完整任务、素材库和对象存储索引。
- `v0.9` 建立社区最小发布模型。
- `v1.0` 完整社区信息流、互动和审核闭环。
- `v1.1+` 将 project / session / version node / job runtime 收敛成稳定的工作台领域模型。

## 存储分层

| 数据 | MVP | 正式版 |
| --- | --- | --- |
| session | Redis / DB | Redis + DB |
| key binding | DB，密文保存 | DB，密文保存 |
| image task | IndexedDB + 服务端摘要 | DB |
| temp asset | BFF 临时资产代理，支持结果下载 | 升级为 image asset |
| image asset | `v0.7` 前不做完整素材库 | DB + Object Storage |
| template | 静态 JSON / DB | DB |
| community work | 不做完整社区 | DB + Object Storage |
| like/favorite/report | 不做完整社区 | DB |
| audit log | 服务端必须有基础记录 | DB / 日志系统 |
| workbench project / session / version node | local-first 或摘要 | DB / IndexedDB + DB |
| board document / board export | local-first | DB + Object Storage |

## 实体关系

```text
user
  -> session
  -> key_binding
  -> workbench_project
      -> creative_session
          -> board_document
              -> board_export
          -> version_node
              -> image_task
                  -> image_asset
              -> job_runtime_event
  -> image_task
      -> image_asset
      -> community_work
  -> template
  -> favorite
  -> report

community_work
  -> image_asset
  -> template
  -> like
  -> favorite
  -> report

project / session / version_node
  -> prompt_snapshot
  -> params_snapshot
  -> board_snapshot
  -> source_asset
  -> output_asset
  -> job_runtime_event
```

## User

来源可以是 Sub2API 导入，也可以后续扩展 PsyPic 自有账号。

字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | PsyPic 用户 id |
| `sub2api_user_id` | string? | Sub2API 用户 id |
| `display_name` | string? | 展示名 |
| `avatar_url` | string? | 头像 |
| `role` | enum | `user/admin` |
| `created_at` | datetime | 创建时间 |
| `updated_at` | datetime | 更新时间 |

MVP 可用匿名导入用户，但必须能绑定 session 和 key binding。

## Session

字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | opaque session id |
| `user_id` | string | 用户 id |
| `key_binding_id` | string? | 当前默认 key binding |
| `expires_at` | datetime | 过期时间 |
| `created_at` | datetime | 创建时间 |
| `last_seen_at` | datetime | 最近访问 |
| `revoked_at` | datetime? | 吊销时间 |

Redis 可保存 session 热数据，DB 保存可审计记录。

## WorkbenchProject

工作台项目是 PsyPic 的一级业务对象，承载商业项目、活动项目、个人项目或同款草稿集。

字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | project id |
| `user_id` | string | 所属用户 |
| `title` | string | 项目名 |
| `sort_order` | int | 排序 |
| `collapsed` | bool | 侧栏折叠状态 |
| `active_session_id` | string? | 当前活跃会话 |
| `created_at` | datetime | 创建时间 |
| `updated_at` | datetime | 更新时间 |

## CreativeSession

一个会话对应一次持续创作线索，通常和项目一一关联，但也可以在 project 下并行存在多个分支会话。

字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | session id |
| `project_id` | string | 所属项目 |
| `title` | string | 会话标题 |
| `fork_parent_version_node_id` | string? | 分叉来源节点 |
| `active_version_node_id` | string? | 当前活跃版本节点 |
| `created_at` | datetime | 创建时间 |
| `updated_at` | datetime | 更新时间 |

## VersionNode

版本节点是工作台的最小回溯单元，生成、编辑、同款和批量展开都应落成节点。

字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | version node id |
| `project_id` | string | 所属项目 |
| `session_id` | string | 所属会话 |
| `parent_version_node_id` | string? | 父节点 |
| `task_id` | string? | 对应任务 |
| `prompt_snapshot` | text | prompt 快照 |
| `params_snapshot` | json | 参数快照 |
| `source_asset_ids` | json | 参考图 / mask / 继续编辑来源 |
| `output_asset_ids` | json | 输出资产 |
| `board_document_id` | string? | 来源 Board Mode |
| `board_snapshot` | json? | 提交时的画布快照 |
| `board_export_asset_id` | string? | 提交给 edits 的扁平化导出资产 |
| `branch_label` | string? | 分支名或标签 |
| `status` | enum | `queued/running/succeeded/failed/canceled/timed_out` |
| `created_at` | datetime | 创建时间 |

## BoardDocument

BoardDocument 是 project/session 下的 Board Mode 文档。它保存图片层、文字层、手绘层和 mask 层的结构化快照，第一阶段可 local-first，服务端化后入库。

字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | board id |
| `project_id` | string | 所属项目 |
| `session_id` | string | 所属会话 |
| `title` | string | 画布标题 |
| `width` | int | 画布宽 |
| `height` | int | 画布高 |
| `background` | json | `transparent/solid/checkerboard` |
| `layers` | json | `BoardLayer[]` |
| `active_layer_id` | string? | 当前选中图层 |
| `source_version_node_ids` | json | 来源版本节点 |
| `source_asset_ids` | json | 来源素材 |
| `preview_asset_id` | string? | 预览缩略图 |
| `created_at` | datetime | 创建时间 |
| `updated_at` | datetime | 更新时间 |

`layers` 只保存 PsyPic 标准 JSON，不保存 Konva Stage 实例。服务端化时需要限制最大图层数、最大点数和最大 JSON 字节数。

## BoardExport

BoardExport 记录一次画布导出。它把 BoardDocument 转换成普通 ImageAsset 或 mask asset，供 `/api/images/edits` 使用。

字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | board export id |
| `board_document_id` | string | 来源画布 |
| `project_id` | string | 所属项目 |
| `session_id` | string | 所属会话 |
| `version_node_id` | string? | 关联版本节点 |
| `kind` | enum | `reference_png/mask_png/preview_png` |
| `asset_id` | string | 导出后的 ImageAsset |
| `width` | int | 导出宽 |
| `height` | int | 导出高 |
| `pixel_ratio` | int | 导出倍率 |
| `created_at` | datetime | 创建时间 |

## JobRuntimeEvent

长任务事件用于对齐 Task Dock、partial preview、审计和排障。

字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | event id |
| `task_id` | string | 任务 |
| `version_node_id` | string? | 版本节点 |
| `event_type` | string | `queued/running/partial_image/succeeded/failed/canceled/timed_out` |
| `payload` | json | 脱敏事件数据 |
| `created_at` | datetime | 创建时间 |

## KeyBinding

Sub2API key 不返回前端。

字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | binding id |
| `user_id` | string | 用户 id |
| `sub2api_base_url` | string | Sub2API base URL |
| `sub2api_api_key_ciphertext` | text? | 加密 API Key |
| `sub2api_api_key_id` | string? | Sub2API key id |
| `default_model` | string | 默认 `gpt-image-2` |
| `enabled_models` | json | 可用模型 |
| `limits` | json | max n、尺寸、moderation 等 |
| `status` | enum | `active/disabled/expired` |
| `created_at` | datetime | 创建时间 |
| `updated_at` | datetime | 更新时间 |

约束：

- `sub2api_api_key_ciphertext` 和 `sub2api_api_key_id` 至少一个存在。
- 加密密钥来自 `KEY_ENCRYPTION_SECRET`。
- 日志不得输出密文字段。

## ImportTicket / ImportCode

短期导入凭证可以优先放 Redis。

字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `code` | string | 一次性导入 code |
| `sub2api_user_id` | string | 来源用户 |
| `sub2api_api_key_id` | string | 来源 key |
| `payload` | json | base_url、limits、model 等 |
| `expires_at` | datetime | 过期时间 |
| `consumed_at` | datetime? | 消费时间 |

要求：

- TTL 30-60 秒。
- 一次性消费。
- `no-store`。
- 不包含明文长期 API Key，除非仅在服务端 Redis 短期密文保存。

## ImageTask

字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | task id |
| `user_id` | string | 用户 id |
| `key_binding_id` | string | key binding |
| `project_id` | string? | 所属项目 |
| `session_id` | string? | 所属会话 |
| `version_node_id` | string? | 所属版本节点 |
| `parent_version_node_id` | string? | 父版本节点 |
| `type` | enum | `generation/edit/mask/stream/same` |
| `status` | enum | `queued/running/succeeded/failed/canceled` |
| `prompt` | text | prompt，可按隐私策略加密或脱敏 |
| `params` | json | size、quality、format、n 等 |
| `commercial_preset` | string? | 商业场景模板 |
| `source_asset_ids` | json | 参考图 |
| `mask_asset_id` | string? | mask |
| `board_document_id` | string? | 来源 Board Mode |
| `board_export_asset_id` | string? | 画布扁平化导出资产 |
| `upstream_request_id` | string? | Sub2API/OpenAI request id |
| `error_code` | string? | 错误码 |
| `error_message` | text? | 脱敏错误 |
| `duration_ms` | int? | 耗时 |
| `created_at` | datetime | 创建时间 |
| `updated_at` | datetime | 更新时间 |

MVP：

- IndexedDB 存完整本地历史。
- 服务端至少可存任务摘要、request id、状态、成本，便于排错和审计。

## TempAsset

MVP 为了支持结果下载和 `/api/assets/{asset_id}`，必须有临时资产代理。它只解决生成结果的短期访问，不等同于正式素材库。

字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | temp asset id |
| `user_id` | string | 所属用户 |
| `task_id` | string | 来源任务 |
| `mime_type` | string | image/png 等 |
| `width` | int? | 宽 |
| `height` | int? | 高 |
| `size_bytes` | int? | 文件大小 |
| `temp_storage_key` | string | 临时文件、临时对象或 Redis blob key |
| `expires_at` | datetime | 过期时间 |
| `created_at` | datetime | 创建时间 |

规则：

- `v0.4` 生成接口可以返回 `/api/assets/{temp_asset_id}`。
- `GET /api/assets/{asset_id}` 先查 TempAsset，再查正式 ImageAsset。
- TempAsset 只能本人访问，除非后续生成分享链接。
- TTL 建议 24-72 小时，用户下载和本地 IndexedDB 历史不依赖长期服务端保存。
- `v0.7` 引入正式 ImageAsset 后，成功生成结果可迁移到对象存储和服务端素材库。

## ImageAsset

`v0.7` 后用于服务端历史、素材库、相册和社区长期访问。

字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | asset id |
| `user_id` | string | 所属用户 |
| `task_id` | string? | 来源任务 |
| `storage_key` | string | 对象存储 key |
| `mime_type` | string | image/png 等 |
| `width` | int | 宽 |
| `height` | int | 高 |
| `size_bytes` | int | 文件大小 |
| `sha256` | string | 去重 hash |
| `asset_kind` | enum | `image/text_effect/reference/mask` |
| `asset_role` | enum | `background/subject/decoration/text_effect/texture_light/shape/brand/reference/mask` |
| `source_kind` | enum | `generated/uploaded/community/template/history` |
| `tags` | json | 用户标签和自动标签 |
| `visibility` | enum | `private/unlisted/public` |
| `created_at` | datetime | 创建时间 |
| `deleted_at` | datetime? | 删除时间 |

规则：

- 私有资产默认不公开访问。
- 公开社区作品也不等于原始素材公开。
- 下载使用短期签名 URL 或 BFF 代理。
- 素材库的分类筛选应以 `asset_role` 为主，`tags` 为辅；TextLayer 不应强行落到 ImageAsset 里。

## BillingUsage

字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | usage id |
| `task_id` | string | 任务 |
| `user_id` | string | 用户 |
| `key_binding_id` | string | key binding |
| `estimated_cost` | decimal | 估算成本 |
| `actual_cost` | decimal? | 实际扣费 |
| `input_tokens` | int? | 输入 token |
| `output_tokens` | int? | 输出 token |
| `total_tokens` | int? | 总 token |
| `quality` | string | quality |
| `size` | string | size |
| `n` | int | 数量 |
| `created_at` | datetime | 创建时间 |

## Template

字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | template id |
| `owner_user_id` | string? | 用户模板为空则系统模板 |
| `name` | string | 模板名 |
| `scene` | string | 商业场景 |
| `description` | text | 描述 |
| `fields_schema` | json | 表单字段 |
| `prompt_template` | text | prompt 模板 |
| `default_params` | json | 默认参数 |
| `status` | enum | `draft/published/archived` |
| `created_at` | datetime | 创建时间 |
| `updated_at` | datetime | 更新时间 |

系统模板必须覆盖：

- 电商主图。
- 产品换背景。
- 广告 Banner。
- 社媒封面。
- 品牌海报。

## CommunityWork

`v0.9` 起建立最小模型。

字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | work id |
| `author_user_id` | string | 作者 |
| `project_id` | string? | 来源项目 |
| `session_id` | string? | 来源会话 |
| `version_node_id` | string? | 来源版本节点 |
| `asset_id` | string | 主图 |
| `task_id` | string? | 来源任务 |
| `template_id` | string? | 来源模板 |
| `title` | string | 标题 |
| `scene` | string | 商业场景 |
| `tags` | json | 标签 |
| `visibility` | enum | `private/unlisted/public` |
| `moderation_status` | enum | `pending/approved/rejected/taken_down` |
| `disclose_prompt` | bool | 是否公开 prompt |
| `disclose_params` | bool | 是否公开参数 |
| `disclose_reference_images` | bool | 是否公开参考图 |
| `allow_same_generation` | bool | 是否允许一键同款 |
| `allow_reference_reuse` | bool | 是否允许他人作为参考图继续编辑 |
| `prompt_snapshot` | text? | 发布时 prompt 快照 |
| `params_snapshot` | json? | 发布时参数快照 |
| `like_count` | int | 点赞数 |
| `favorite_count` | int | 收藏数 |
| `report_count` | int | 举报数 |
| `featured_at` | datetime? | 精选时间 |
| `published_at` | datetime? | 发布时间 |
| `created_at` | datetime | 创建时间 |
| `updated_at` | datetime | 更新时间 |

规则：

- 默认 `private`。
- `public` 必须二次确认。
- prompt、参数、参考图默认不公开。
- 下架不删除原资产，只隐藏社区展示。

## CommunityInteraction

点赞和收藏可以共用表，也可拆表。

字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | id |
| `work_id` | string | 作品 |
| `user_id` | string | 用户 |
| `type` | enum | `like/favorite` |
| `created_at` | datetime | 创建时间 |

唯一约束：

- `(work_id, user_id, type)` 唯一。

## CommunityReport

字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | report id |
| `work_id` | string | 作品 |
| `reporter_user_id` | string | 举报人 |
| `reason` | enum | `copyright/privacy/sensitive/spam/other` |
| `description` | text? | 补充说明 |
| `status` | enum | `open/reviewing/resolved/rejected` |
| `handled_by` | string? | 管理员 |
| `handled_at` | datetime? | 处理时间 |
| `created_at` | datetime | 创建时间 |

## AuditLog

字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | log id |
| `actor_user_id` | string? | 操作者 |
| `action` | string | 操作 |
| `target_type` | string | 目标类型 |
| `target_id` | string | 目标 id |
| `request_id` | string | 请求 id |
| `ip_hash` | string? | IP hash |
| `user_agent_hash` | string? | UA hash |
| `metadata` | json | 脱敏元数据 |
| `created_at` | datetime | 创建时间 |

不得记录：

- 明文 API Key。
- 完整 cookie。
- 原始图片二进制。
- 未经脱敏的敏感 prompt。

## IndexedDB 草案

MVP 本地表：

```text
local_tasks
local_assets
local_board_documents
local_settings
local_templates_cache
```

`local_tasks`：

- `id`
- `prompt`
- `params`
- `thumbnail_blob`
- `asset_blob_refs`
- `status`
- `created_at`

`local_board_documents`：

- `id`
- `project_id`
- `session_id`
- `title`
- `layers`
- `source_asset_ids`
- `source_version_node_ids`
- `updated_at`

本地数据只用于体验，不作为计费、审计和安全依据。

## 迁移顺序

1. `v0.2`：User、Session、KeyBinding、AuditLog。
2. `v0.3-v0.4`：ImageTask 摘要、BillingUsage 摘要。
3. `v0.4`：TempAsset，支撑 `/api/assets/{asset_id}` 和 MVP 结果下载。
4. `v0.5`：Job/Task 状态扩展。
5. `v0.7`：ImageAsset、服务端历史和对象存储。
6. `v0.8`：Template。
7. `v0.9`：CommunityWork、CommunityReport。
8. `v1.0`：CommunityInteraction、精选、完整信息流索引。
9. `v1.1+`：WorkbenchProject、CreativeSession、VersionNode、JobRuntimeEvent、BoardDocument、BoardExport、搜索索引和 project/session 关联字段。

## 索引建议

- `session.user_id`
- `key_binding.user_id,status`
- `image_task.user_id,created_at`
- `image_task.status,created_at`
- `temp_asset.user_id,expires_at`
- `image_asset.user_id,created_at`
- `board_document.project_id,updated_at`
- `board_export.board_document_id,created_at`
- `community_work.visibility,moderation_status,published_at`
- `community_work.scene,published_at`
- `community_interaction.work_id,type`
- `community_report.status,created_at`

## 待确认

- prompt 是否需要服务端加密存储。
- 图片默认保留周期。
- 用户删除任务时是否物理删除图片。
- 社区公开作品是否需要默认审核。
- 是否允许匿名手动 key 用户发布公开作品。
